@model List<ReportModel>

@{
    ViewData["Title"] = "Lista raportów";

    ViewData["Page"] = "ReportView";
    ViewData["Group"] = "Report";

    Layout = "~/Areas/Dashboard/Views/Shared/_Layout.cshtml";


}
<div class="page-inner">
    <div class="page-header">
        <h4 class="page-title">Wrzuc tu nazwe</h4>
        <ul class="breadcrumbs">
            <li class="nav-home">
                <a href="@Url.Action("Index","Dashboard")">
                    <i class="flaticon-home"></i>
                </a>
            </li>
            <li class="separator">
                <i class="flaticon-right-arrow"></i>
            </li>
            <li class="nav-item">
                <a href="@Url.Action("List","Report")">Raporty</a>
            </li>
            <li class="separator">
                <i class="flaticon-right-arrow"></i>
            </li>
            <li class="nav-item">
                Szczegóły
            </li>
        </ul>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <div class="d-flex align-items-center">
                        <h4 class="card-title">Raporty</h4>
                    </div>
                </div>
                <div class="card-body">
                    <div class="row">




                        @if (Model.Count != 0)
                            @foreach (var reportModel in Model)
                            {

                                <div class="col-md-6">
                                    <div class="card" style="min-height: 500px; margin: 15px;">

                                        @if (reportModel.ReportImage == null)
                                        {
                                            <img class="card-img-top"
                                                 src="~/images/img-plcaeholder.png"
                                                 alt="Card image cap">
                                        }
                                        else
                                        {
                                            <img class="card-img-top"
                                                 src="@reportModel.ReportImage.Url"
                                                 alt="Card image cap"
>
                                        }


                                        <div class="card-body">
                                            @if (reportModel.Title != null)
                                            {
                                                <h5 class="card-title">@reportModel.Title</h5>
                                            }
                                            else
                                            {
                                                <h5 class="card-title">Brak danych</h5>
                                            }

                                            @if (reportModel.Spider.AnimalTaxonomy.Genus == null)
                                            {
                                                <p class="card-text">Brak danych</p>
                                            }
                                            else
                                            {
                                                <p class="card-text">@reportModel.Spider.AnimalTaxonomy.Genus</p>
                                            }

                                        </div>
                                        <ul class="list-group list-group-flush">
                                            <li class="list-group-item">Data dodania: @reportModel.AddDate.ToShortDateString()</li>
                                        </ul>
                                        <div class="card-body">
                                            <a href="@Url.Action("Details","Report", new { Id = reportModel.Id})" class="card-link">Czytaj więcej..</a>
                                        </div>
                                    </div>
                                </div>
                            }

                    </div>
                </div>


            </div>
        </div>


    </div>
</div>

@section Scripts {
    <script>


    </script>

}